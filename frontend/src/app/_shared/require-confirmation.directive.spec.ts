import { RequireConfirmationValidatorDirective } from './require-confirmation.directive';

describe('RequireConfirmationDirective', () => {
  it('should create an instance', () => {
    const directive = new RequireConfirmationValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
